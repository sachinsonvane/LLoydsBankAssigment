<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.MainActivity">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="10dp">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingBottom="7dp">

                <ImageView
                    android:id="@+id/logoImg"
                    android:layout_width="@dimen/logo_img_w"
                    android:layout_height="@dimen/logo_img_h"
                    android:src="@drawable/logo_img"
                    />
                <TextView
                    android:id="@+id/logoImgTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="@dimen/title_font_size_20"
                    android:textColor="@color/option_title_color"
                    android:text="@string/logo_title"
                    android:layout_marginLeft="@dimen/margin_left_10"
                    android:textStyle="bold|italic"
                    android:layout_toRightOf="@+id/logoImg"
                    />

            </RelativeLayout>

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/moptionsRecyView"
                android:layout_width="match_parent"
                android:layout_marginTop="@dimen/optopn_rcy_v_margin_top"
                android:layout_height="match_parent" />

        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>